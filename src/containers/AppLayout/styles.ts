import { createStyles, makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() =>
  createStyles({
    applayout: {
      width: '100%',
      backgroundColor: '#fff',
    },
  }),
);
